import { AdditionValidators } from './addition-validators';

describe('AdditionValidators', () => {
  it('should create an instance', () => {
    expect(new AdditionValidators()).toBeTruthy();
  });
});
